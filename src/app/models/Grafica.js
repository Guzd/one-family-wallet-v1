class Grafica {
    constructor(id, title) {
        this.id = id;
        this.title = title;
        this.tag = tag;
    }

    generateGraph(gastos, type) {
        // UI implementation pending
    }
}

export default Grafica;
